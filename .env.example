# Neon Postgres connection string (use the pooled connection for serverless)
# Get this from https://console.neon.tech (Connection Details → Pooled connection)
DATABASE_URL="postgresql://user:password@ep-xxx-pooler.region.aws.neon.tech/neondb?sslmode=require"

# Direct (non-pooled) URL — used by Prisma migrations
DIRECT_URL="postgresql://user:password@ep-xxx.region.aws.neon.tech/neondb?sslmode=require"

# Auth.js (NextAuth v5)
# Generate with: openssl rand -base64 32
AUTH_SECRET=""
# Only needed in development. Vercel auto-detects in production via VERCEL_URL.
AUTH_URL="http://localhost:3000"

# Google OAuth
# Create at https://console.cloud.google.com/apis/credentials
# Authorized redirect URI must be: <AUTH_URL>/api/auth/callback/google
AUTH_GOOGLE_ID=""
AUTH_GOOGLE_SECRET=""
